interface:
  display_name: "To PRD"
  short_description: "Turn conversation context into a PRD"
  default_prompt: "Use $to-prd to synthesize this discussion into a focused product requirements document."
