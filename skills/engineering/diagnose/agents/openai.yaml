interface:
  display_name: "Diagnose"
  short_description: "Debug hard failures with a disciplined loop"
  default_prompt: "Use $diagnose to reproduce, minimize, instrument, fix, and regression-test this failure."
